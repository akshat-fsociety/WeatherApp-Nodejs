{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "Weather App which dynamically fetch data and populates it.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Akshat",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "requests": "^0.3.0"
  }
}
